#include <bits/stdc++.h>

std::vector <long long> v;

void a_sqrt () {
  for (int i = 1; i * i <= (int)1e9; i++)
    v.push_back (i * i);
}

int main () {
  long long n; scanf ("%lld", &n);


  return 0;
}
