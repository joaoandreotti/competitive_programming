#include <bits/stdc++.h>

void input ();
void solve ();

typedef long long ll;

const int MAXN = INT_MAX, MAXP = 1e8;

ll m, n, s;
ll primes [MAXP];

void input () {
	scanf ("%lld%lld%lld", &m, &n, &s);
	solve ();
}

void solve () {
	printf ("%lld\n", r ());
}

int main () {
	input ();
	return 0;
}
